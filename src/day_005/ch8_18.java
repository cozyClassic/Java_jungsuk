package day_005;

public class ch8_18 {
    public static void main(String args[]) {

    }
    /*
    static void StartInstall() throws SpaceException, MenoryException {
        if (!enoughSpace()){
            throw new SpaceException("설치할 공간이 부족합니다.");
        }
        if (!enoughMemory()){
            throw new MenoryException("메모리가 부족합니다.");
        }
    }

    static void StartInstall2() throws SpaceException { // 선언부에 MemoryException을 제외해도 됨.
        if (!enoughSpace()){
            throw new SpaceException("설치할 공간이 부족합니다.");
        }
        if (!enoughMemory()){
            throw new RuntimeException(new MemoryException("메모리가 부족합니다."));
        }
    }

     */
}
